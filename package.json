{
  "name": "casdoor-next-auth-kit-monorepo",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm --filter demo dev",
    "build": "pnpm -r build",
    "type-check": "pnpm build:auth-kit && pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "check": "pnpm --filter demo check",
    "build:auth-kit": "pnpm --filter @foldspace-fe/casdoor-next-auth-kit build",
    "build:demo": "pnpm --filter demo build",
    "skill:install": "node scripts/install-skill.mjs"
  }
}
